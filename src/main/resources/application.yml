
quarkus:
   datasource:
      db-kind: mysql 
      jdbc:
        driver: com.mysql.jdbc.Driver
        url:  jdbc:mysql://1.1.1.1:8080/sas
      username: ssa
      password: ssd
# drop and create the database at startup (use `update` to only update the schema)
   hibernate-orm:
      database:
         generation: update
